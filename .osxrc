# osx same directory in shell
precmd () {print -Pn "\e]2; %~/ \a"}
preexec () {print -Pn "\e]2; %~/ \a"}

export CLICOLOR=1
export LSCOLORS=GxFxCxDxBxegedabagaced

alias vi='mvim'
alias vim='mvim'
alias apts='homebrew search'
